name: Basic CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 24f1000743@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Finish
        run: echo "Workflow completed"
